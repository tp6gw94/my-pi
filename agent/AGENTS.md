# Code edit style

- Do not add comments that restate what the code already says.
- Do not narrate the change ("// fix bug", "// add validation", "// updated").
- Keep comments only for non-obvious intent, invariants, tradeoffs, or external constraints.
- Prefer deleting stale comments over rewriting them.
